<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:paddingStart="24dp"
    android:paddingEnd="24dp"
    android:paddingTop="10dp"
    android:paddingBottom="0dp"> <!-- Adjust padding as needed -->

    <TextView
        android:id="@+id/dialog_message_textview"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textAppearance="?android:attr/textAppearanceSmall"
        android:textColor="?android:attr/textColorSecondary"
        android:text="@string/dialog_message_image_input_mode"
        android:layout_marginBottom="16dp" />

    <!-- Row 1: Camera Button (Full Width) -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/dialog_button_camera"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:minHeight="48dp"
        android:layout_marginBottom="8dp"
        android:text="@string/dialog_option_camera"
        android:textSize="14sp"
        android:backgroundTint="@color/primary"
        android:textColor="@color/primary_foreground"
        app:cornerRadius="8dp" />

    <!-- Row 2: Cancel and Gallery Buttons (Side-by-side) -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:layout_marginBottom="8dp"> <!-- Add bottom margin for spacing -->

        <com.google.android.material.button.MaterialButton
            android:id="@+id/dialog_button_cancel"
            style="@style/Widget.Material3.Button.TextButton.Dialog"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:minHeight="48dp"
            android:layout_marginEnd="8dp"
            android:text="@string/action_cancel"
            android:textColor="@color/muted_foreground" />

        <com.google.android.material.button.MaterialButton
            android:id="@+id/dialog_button_gallery"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:minHeight="48dp"
            android:text="@string/dialog_option_gallery"
            android:textSize="14sp"
            android:backgroundTint="@color/border"
            android:textColor="@color/primary"
            app:cornerRadius="8dp" />

    </LinearLayout>
</LinearLayout>